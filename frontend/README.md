# Campus Network Frontend

校园网络算法可视化平台前端应用，提供直观的交互界面和实时可视化效果。

## ✨ 技术栈

- **框架**: Vue 3 Composition API
- **构建工具**: Vite 5.0
- **可视化**: 原生SVG + CSS 动画
- **状态管理**: provide/inject 模式
- **样式方案**: 现代CSS + 响应式布局

## 🚀 功能特性

### 核心算法面板
- 🌲 **最小生成树**: Kruskal/Prim 算法对比、步骤动画、性能指标
- 💧 **最大流**: Edmonds-Karp/Dinic 算法对比、增广路径可视化
- 🔐 **AES加密**: AES-128 加密/解密界面 (hex 格式)

### 网络管理
- 🌐 **网络配置**: 随机生成、手动编辑、默认配置管理
- 🖼️ **拓扑预览**: 高质量网络图生成与显示
- ✏️ **边编辑器**: 可视化编辑网络边的权重、容量等属性

### 交互式仿真
- 🎮 **实时仿真**: SVG 动画演示流量传输过程
- 📊 **负载均衡**: 单路径/多路径策略选择
- 🚦 **拥塞监控**: 链路利用率实时显示、拥塞预警
- 💯 **统计面板**: 实时流量统计、平均利用率、活跃路径数

## 安装与启动
```bash
npm install
npm run dev
```
访问：`http://localhost:3000`

后端需先启动：
```bash
cd ../backend
python app.py
```

## 前端调用的后端 API
见 `src/api/backend.js`，主要封装：
- `mstCompare(nodes, edges)` → POST /api/mst/compare
- `maxflowEdmondsKarp(nodes, edges, source, sink)` → POST /api/maxflow/edmonds-karp
- `maxflowDinic(nodes, edges, source, sink)` → POST /api/maxflow/dinic
- `aesEncrypt(plaintext, key)` / `aesDecrypt(encryptedHex, key)`
- `getDefaultNetworkConfig()` / `generateNetwork(config)`
- `previewGraph(nodes, edges, labelMode)`
- `calculateTrafficPaths(nodes, edges, source, target, totalFlow, strategy, numPaths)`

## 📁 项目结构

```
frontend/
├── index.html              # HTML 入口文件
├── package.json            # NPM 依赖配置
├── vite.config.js          # Vite 构建配置
└── src/
    ├── main.js             # 应用入口文件
    ├── App.vue             # 根组件（Tab切换、全局状态）
    ├── api/
    │   └── backend.js      # 后端API封装
    └── components/         # Vue 组件
        ├── NetworkConfigPanel.vue       # 网络配置与生成
        ├── MSTPanel.vue                 # 最小生成树算法面板
        ├── MaxFlowPanel.vue             # 最大流算法面板
        ├── InteractiveTrafficPanel.vue  # 交互式流量仿真
        ├── AESPanel.vue                 # AES加密面板
        ├── EdgeEditor.vue               # 边编辑器组件
        ├── AnimationPlayer.vue          # 动画播放器
        ├── ImageViewer.vue              # 图像查看器
        └── Toast.vue                    # 通知组件
```

## 📦 组件说明

### 主组件

#### App.vue
- 功能：根组件，管理Tab切换和全局状态
- 状态管理：provide/inject 模式共享 `globalNetwork`
- Tab页：网络配置、MST、最大流、交互仿真、AES加密

#### NetworkConfigPanel.vue
- 功能：网络拓扑配置与生成
- 模式：自动生成 / 手动编辑
- API：`/api/network/generate`、`/api/graph/preview`
- 特点：支持应用为默认配置，保存至localStorage

#### MSTPanel.vue
- 功能：最小生成树算法对比
- 输入模式：文本输入 / 可视化输入
- API：`/api/mst/compare`
- 显示：算法性能、步骤动画、结果可视化

#### MaxFlowPanel.vue
- 功能：最大流算法对比
- 输入模式：文本输入 / 可视化输入
- API：`/api/maxflow/edmonds-karp`、`/api/maxflow/dinic`
- 显示：EK/Dinic对比、增广路径动画、流量分配

#### InteractiveTrafficPanel.vue
- 功能：交互式流量仿真
- 特性：SVG动画、实时统计、拥塞检测
- 策略：单路径 / 负载均衡
- 交互：开始/暂停/继续/重置、缩放/拖拽

#### AESPanel.vue
- 功能：AES-128加密和解密
- API：`/api/aes/encrypt`、`/api/aes/decrypt`
- 格式：hex编码字符串

### 通用组件

#### EdgeEditor.vue
- 功能：可视化编辑网络边
- 支持：添加/删除/修改边，编辑权重/容量
- 使用：MSTPanel、MaxFlowPanel、NetworkConfigPanel

#### AnimationPlayer.vue
- 功能：算法步骤动画播放
- 控制：播放/暂停、上一步/下一步、速度调节
- 显示：步骤图像 + 描述文本

#### ImageViewer.vue
- 功能：大图查看器
- 特性：遵罩层、缩放、关闭按钮
- 支持：Base64图像显示

#### Toast.vue
- 功能：通知消息组件
- 类型：success / error / warning / info
- 特性：自动消失、手动关闭

## 🔌 API调用 (api/backend.js)

### 网络管理
```javascript
getDefaultNetworkConfig()           // 获取默认配置
generateNetwork(config)             // 生成随机网络
previewGraph(nodes, edges, mode)    // 绘制原始图
```

### 算法调用
```javascript
mstCompare(nodes, edges)                        // MST对比
maxflowEdmondsKarp(nodes, edges, src, sink)     // EK算法
maxflowDinic(nodes, edges, src, sink)           // Dinic算法
aesEncrypt(plaintext, key)                      // AES加密
aesDecrypt(ciphertext, key)                     // AES解密
```

### 交互仿真
```javascript
calculateTrafficPaths(
    nodes, edges, source, target,
    totalFlow, strategy, numPaths, edgeUsage
)
```

## 📚 使用指南

### 基础使用

1. **启动应用**
   ```bash
   npm install
   npm run dev
   ```

2. **配置网络**
   - 进入"网络配置"页面
   - 点击"随机生成"生成网络
   - 点击"应用到算法"保存配置

3. **运行算法**
   - 切换到相应算法面板
   - 点击"加载配置网络"或手动输入
   - 点击"计算"运行算法
   - 查看结果和动画

4. **交互仿真**
   - 进入"交互式仿真"页面
   - 设置源/目标节点、流量速率
   - 选择策略（单路径/负载均衡）
   - 点击"开始"运行仿真

### 高级功能

- **动画播放**: 点击步骤图片进入动画播放模式
- **图像查看**: 点击结果图片放大查看
- **数据导出**: 右键保存图片或复制数据
- **参数调整**: 修改网络参数观察影响

## ⚠️ 注意事项

1. **后端依赖**: 必须先启动后端服务 (http://localhost:5000)
2. **浏览器兼容**: 推荐使用Chrome/Edge/Firefox现代浏览器
3. **AES格式**: 解密时必须输入hex格式密文
4. **网络配置**: 交互仿真需要先配置网络拓扑
5. **性能考虑**: 大型网络(>50节点)可视化可能较慢

## 🛠 开发指南

### 构建命令
```bash
npm run dev      # 开发模式
npm run build    # 生产构建
npm run preview  # 预览构建结果
```

### 添加新组件
1. 在 `src/components/` 创建 `.vue` 文件
2. 在 `App.vue` 注册组件
3. 添加到 Tab 列表
4. 如需API调用，在 `api/backend.js` 添加接口

### 状态管理
- 使用 `inject('globalNetwork')` 获取全局网络
- 使用 `inject('setGlobalNetwork')` 更新全局网络
- localStorage 用于持久化存储配置
